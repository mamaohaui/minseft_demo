/**app.wxss**/
/* Apple HIG（iOS 设计规范）全局基础样式。

   设计令牌（与各页面 wxss 保持一致，改动请全局搜索同步）：
   - 分组背景  #f2f2f7   (systemGroupedBackground)
   - 卡片       #ffffff   圆角 24rpx
   - 主文字     #1c1c1e   (label)
   - 次要文字   #8e8e93   (secondaryLabel)
   - 三级文字   #c7c7cc   (tertiaryLabel)
   - 发丝线     rgba(60,60,67,0.12)
   - 强调绿     #34c759   (systemGreen，主操作)
   - 强调蓝     #007aff   (systemBlue，查看/链接类)
   - 强调橙     #ff9500   (systemOrange，评分/警示)
   - 强调红     #ff3b30   (systemRed，删除/驳回)
   - 卡片阴影   0 8rpx 24rpx rgba(0,0,0,0.06)

   注意：不要在全局定义 .container 这类通用类名的布局（height/flex 等），
   会与各页面自己的 .container 样式部分叠加，导致页面无法滚动、内容错位。
   页面级容器样式一律写在各自页面的 wxss 里。 */
page {
  background: #f2f2f7;
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue",
    "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  font-size: 28rpx;
  color: #1c1c1e;
  -webkit-font-smoothing: antialiased;
}
